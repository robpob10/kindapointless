// Who the game is about. Change this one line and the whole site updates.
export const NAME = '<name>';
export const TITLE = `${NAME} Pointless`;

// Namespace for rows in Postgres, so multiple games can share one database.
export const GAME_ID = 'kinda';
