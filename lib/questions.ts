// The BASE questions used in the data gathering phase (/collect).
//
// This ships empty — attendees add their own questions on /collect, which are
// stored in Postgres (custom_questions) and merged after this list. Add any
// built-in questions here if you want some from the start.
export const QUESTIONS: string[] = [];
