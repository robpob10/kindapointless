# Vercel Postgres - copy these from your Vercel project's Storage tab
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# The /admin export + reset are gated by a hardcoded key ("banana") in the API
# routes - nothing to set here.
